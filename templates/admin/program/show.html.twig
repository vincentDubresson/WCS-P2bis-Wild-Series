{% extends 'base.html.twig' %}

{% block title %}Admin - Série{% endblock %}

{% block body %}

    {% include '/admin/layouts/_admin_navbar.html.twig' %}

    
        <section class="adminList">

        <h1>Program</h1>

        <div class="container d-inline">
            <button class="btn btn-dark"><a class="text-light text-decoration-none" href="{{ path('app_program_index') }}">Retour</a></button>
            <button class="btn btn-dark"><a class="text-light text-decoration-none" href="{{ path('app_program_edit', {'programSlug': program.slug}) }}">Modifier</a></button>
        </div>

        <div class="adminTable">

            <table class="table">
                <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ program.id }}</td>
                    </tr>
                    <tr>
                        <th>Title</th>
                        <td>{{ program.title }}</td>
                    </tr>
                    <tr>
                        <th>Synopsis</th>
                        <td>{{ program.synopsis }}</td>
                    </tr>
                    <tr>
                        <th>Poster</th>
                        <td>{{ program.poster }}</td>
                    </tr>
                </tbody>
            </table>

        </div>

    </section>

    {{ include('/admin/program/_delete_form.html.twig') }}
{% endblock %}
